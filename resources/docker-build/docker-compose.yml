services:
    emscripten-build:
        image: eez-studio-lvgl-build:latest
        build: .
        volumes:
            - project-volume:/project
        working_dir: /project
        command: bash

volumes:
    project-volume:
        name: ${PROJECT_VOLUME:-eez-lvgl-build-default}
